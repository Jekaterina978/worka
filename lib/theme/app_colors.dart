import 'package:flutter/material.dart';

class AppColors {
  // Менять здесь
  static const Color primaryBlue = Color(0xFF578CEC);

  static const Color bg = Colors.white;

  static const Color textPrimary = Color(0xFF111111);
  static const Color textSecondary = Color(0xFF4B4B4B);

  static const Color pillInactiveBg = Color(0xFFEDEDED);
  static const Color hoverBlueSoft = Color(0x1A578CEC); // светло-синяя подсветка

  // Звезда избранного
  static const Color favoriteOn = Color(0xFFFFC107); // желтая
  static const Color favoriteOff = Color(0xFFBDBDBD);

  // Текст на кнопках всегда белый
  static const Color onColored = Colors.white;
}
